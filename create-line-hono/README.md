# create-linebot-hono

Scaffold a LINE bot project powered by `linebot-hono`.

## Usage

```sh
npm create linebot-hono@latest my-bot
```

or:

```sh
npx create-linebot-hono@latest my-bot
```

## Templates

- `worker-ts` (default): Cloudflare Workers + TypeScript + Wrangler

